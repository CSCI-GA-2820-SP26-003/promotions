apiVersion: tekton.dev/v1
kind: Pipeline
metadata:
  name: promotion-cd-pipeline
spec:
  params:
    - name: GIT_REPO
      type: string
      description: URL of the git repo
      default: https://github.com/CSCI-GA-2820-SP26-003/promotions.git
    - name: GIT_REF
      type: string
      description: Target branch or sha of repo
      default: master
    - name: APP_NAME
      type: string
      description: Name of app
      default: promotion
    - name: IMAGE_NAME
      type: string
      description: Image to build and push
      default: image-registry.openshift-image-registry.svc:5000/$(context.pipelineRun.namespace)/$(params.APP_NAME):latest
    - name: BASE_URL 
      type: string
      description: Route URL for the deployed app
      default: https://promotion-jkim231-dev.apps.rm3.7wse.p1.openshiftapps.com
  workspaces:
    - name: pipeline-workspace

  tasks:
    - name: git-clone
      taskRef:
        resolver: cluster
        params:
          - name: kind
            value: task
          - name: name
            value: git-clone
          - name: namespace
            value: openshift-pipelines
      workspaces:
        - name: output
          workspace: pipeline-workspace
      params:
        - name: URL
          value: $(params.GIT_REPO)
        - name: REVISION
          value: $(params.GIT_REF)
        - name: DEPTH
          value: "1"
        - name: SSL_VERIFY
          value: "true"
        - name: SUBMODULES
          value: "true"
        - name: DELETE_EXISTING
          value: "true"
        - name: VERBOSE
          value: "false"
        - name: USER_HOME
          value: /home/git

    - name: lint
      taskRef:
        name: pylint
      workspaces:
        - name: source
          workspace: pipeline-workspace
      params:
        - name: path
          value: service
      runAfter:
        - git-clone

    - name: test
      taskRef:
        name: pytest-env
      workspaces:
        - name: source
          workspace: pipeline-workspace
      runAfter:
        - git-clone

    - name: buildah
      taskRef:
        resolver: cluster
        params:
          - name: kind
            value: task
          - name: name
            value: buildah
          - name: namespace
            value: openshift-pipelines
      workspaces:
        - name: source
          workspace: pipeline-workspace
      params:
        - name: IMAGE
          value: $(params.IMAGE_NAME)
        - name: DOCKERFILE
          value: ./Dockerfile
        - name: BUILD_ARGS
          value:
            - ""
        - name: CONTEXT
          value: .
        - name: STORAGE_DRIVER
          value: vfs
        - name: FORMAT
          value: oci
        - name: BUILD_EXTRA_ARGS
          value: ""
        - name: PUSH_EXTRA_ARGS
          value: ""
        - name: SKIP_PUSH
          value: "false"
        - name: TLS_VERIFY
          value: "true"
        - name: VERBOSE
          value: "false"
      runAfter:
        - lint
        - test

    - name: deploy
      taskRef:
        name: deploy-image
      workspaces:
        - name: source
          workspace: pipeline-workspace
      params:
        - name: app-name
          value: $(params.APP_NAME)
        - name: image-name
          value: $(params.IMAGE_NAME)
      runAfter:
        - buildah

    - name: bdd
      taskRef:
        name: behave
      workspaces:
        - name: source
          workspace: pipeline-workspace
      params:
        - name: base-url
          value: $(params.BASE_URL)
      runAfter:
        - deploy